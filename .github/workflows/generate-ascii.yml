name: Generate ASCII Artwork

on:
  push:
    branches:
      - main
      - dev

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:  # ✅ debe ser 'steps'
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Execcuting Shell Script
        run: |
          ls -ltra
          ./ascii-script.sh
